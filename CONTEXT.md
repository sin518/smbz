# CONTEXT.md

## 项目领域

本项目是一个综合性的中国传统占术（命理）在线服务平台，涵盖多种占术系统。

## 核心占术系统

### 八字（四柱命理）
基于出生年月日时的天干地支排盘，分析命局、大运、流年。包含十神、神煞、格局、喜用神等概念。

### 紫微斗数
基于出生时间和农历日期的斗数排盘系统，涉及十二宫位、主星、辅星、四化（化禄、化权、化科、化忌）等。

### 奇门遁甲
时空占卜系统，基于节气、时辰排出九宫格局，包含天盘、地盘、人盘、神盘等多层信息。

### 六爻
摇卦占卜系统，通过卦象（六个爻位）、世应、六亲、六神等要素进行分析。

### 大六壬
基于天干地支的时空占卜体系，涉及四课、三传、天将等复杂推演。

## 关键业务规则

1. **确定性计算 vs AI 解读分离**：排盘计算（天干地支、星曜位置、卦象生成）必须是确定性的、可复现的；AI 解读是独立的增值服务层。

2. **前端计算优先**：占术排盘逻辑在前端（`packages/core`）完成，避免服务端计算负载，提升用户体验。

3. **用户数据隔离**：每个用户只能访问自己创建的盘局、解读记录，后端 API 必须严格按 user_id 过滤。

4. **会员与积分体系**：
   - 免费用户：有限次数的 AI 解读
   - 会员用户：更多或无限次数的 AI 解读
   - 积分扣减：AI 解读消耗积分，必须在调用 AI 前扣减，失败时退款

5. **多端一致性**：Web 端和移动端（响应式）必须展示一致的排盘结果和解读内容。

## 技术架构关键点

- **Frontend**: Next.js App Router、React Server Components 为主、客户端组件按需使用
- **Backend**: FastAPI、asyncpg（直接 SQL）、可选 Redis 缓存
- **AI 层**: 通过 `divination-pipeline.ts` 统一管理 AI 解读流程（鉴权→积分→AI 调用→持久化）
- **数据库**: Supabase PostgreSQL，使用参数化 SQL，禁止拼接用户输入

## 常见术语

- **排盘**：根据用户输入（出生时间、起卦信息等）生成占术图表的过程
- **盘局**：一次排盘的结果数据
- **盘局记录**：用户保存的一份确定性盘局，统一适用于八字、六爻、奇门遁甲、紫微斗数和大六壬。同一账号下，记录身份由求测主体姓名和各占术的规范化排盘输入共同确定；不同姓名或不同真太阳时设置属于不同记录，等价的公历与农历录入、同一行政区与规范化坐标的地点文字差异、电脑、手机、浏览器及设备本机 ID 不影响记录身份。避免称作“排盘事件”。
- **起卦记录**：一次已经完成的六爻起卦及其盘局。同一问题在不同时间分别完成的起卦属于不同记录，即使六爻结果完全相同；同一次起卦在不同设备上的副本属于同一记录。
- **时占盘局记录**：一次奇门遁甲或大六壬起局形成的盘局记录。同一账号下，占事、起局时间、出生年及排盘配置全部规范化相同时属于同一记录；任一身份输入不同则属于不同记录。
- **记录浏览**：从记录列表打开已有盘局的只读行为，不修改记录内容、时间、同步状态或排序，也不触发再次保存。避免称作“重新排盘”或“重新保存”。
- **重新提交**：用户明确提交与已有盘局记录身份相同的排盘，更新原记录内容和更新时间，但保留创建时间；记录按最近更新时间移动到列表顶部。避免称作“新增记录”。
- **排盘计算版本**：生成确定性盘局所使用的规则版本，不属于盘局记录身份。同一规范化输入因计算版本升级产生修正结果时更新原记录，并保留版本信息用于追溯；同步冲突时旧计算版本不得覆盖新版本。
- **记录删除**：用户删除已同步的盘局记录后，该记录在所有设备上均保持删除；离线设备残留的旧副本不得在恢复同步后使记录复活。
- **记录重新创建**：用户在记录删除后明确提交相同规范化排盘输入，开始该盘局记录的新生命周期。重新创建不同于旧离线副本的自动同步，后者不得使已删除记录复活。
- **重复盘局记录**：同一账号下具有相同记录身份的多份存储副本，领域上仍是一条盘局记录。系统不支持同时存在的完全相同副本；重复副本应自动合并，保留最早创建时间以及最新内容和更新时间。
- **本机记录空间**：当前设备上按账号隔离保存盘局记录的区域；游客拥有独立空间。一个账号不得查看或同步其他账号遗留在同一浏览器中的本机记录，游客记录仅可在用户明确同意后导入当前账号。
- **账号退出**：结束当前账号对其本机记录空间的访问。普通退出保留但隐藏该账号的本机数据；用户可明确选择退出并清除此设备上的账号数据，其他账号和游客始终不可读取。
- **待认领记录**：旧版本遗留且无法确定账号归属的本机盘局记录。用户明确确认前，待认领记录不属于任何账号，不在账号记录中展示，也不得同步到云端。
- **未同步记录**：已在本机保存、但尚未确认云端持有副本的盘局记录，是当前唯一可信副本。系统不得因数量或容量限制静默删除未同步记录；已确认同步的本机缓存可以按容量策略淘汰。
- **记录摘要**：用于记录列表展示的盘局身份、标题、类型、时间和同步状态等少量信息，不包含完整盘局内容。登录后的设备先同步记录摘要，再按需获取完整盘局。
- **离线盘局副本**：设备打开云端记录后缓存，或由用户明确下载以供离线浏览的完整盘局内容。普通缓存可以按容量策略淘汰；用户明确下载的固定离线副本不得静默清理，云端盘局记录本身不受影响。
- **记录同步**：盘局记录在本机保存成功后，登录状态下立即尝试上传云端；网络失败不影响本机保存，联网事件和定时任务负责重试待同步记录。避免把定时任务作为正常同步的首次触发。
- **记录新鲜度**：当前设备展示的记录摘要与云端最新状态的一致程度。云端读取失败时继续展示最后缓存和本机记录，并明确标记数据可能不是最新；不得把连接失败解释为云端没有记录。
- **解读/解盘**：对排盘结果进行文字化分析和解释
- **AI 解读**：使用大语言模型对盘局进行智能分析
- **求测主体**：一次盘局主要分析的人，其出生年份用于确定奇门年命；页面操作者或事项相关方不一定是求测主体。避免笼统称作“用户”或“提问者”。
- **奇门年命**：求测主体所填公历出生年份对应的天干，是奇门解读中的长期个人坐标，不校正立春边界；甲命按该出生年完整干支对应的六甲遁干定位，并以天盘定位干所在宫作为年命宫，地盘同干仅作背景参考。日干表示起局当下的自身状态，时干表示所问之事，年命必须参看但不能单独推翻主要结论。避免称作“流年”“命宫”或“精确四柱年柱”。
- **天盘长生**：天盘干在当前宫位所辖地支上的十二长生状态；四隅宫分别保留两个地支结果，寄宫出现多个天盘干时分别计算。它是辅助旺衰证据，不能单独决定吉凶。避免笼统称作“宫位长生”或与入墓状态混用。
- **大六壬天地盘**：由外围十二个地支位置构成，展示各地盘位置所临天盘地支、天将、遁干、旺衰和十二长生；它不是奇门遁甲的九宫。界面使用“天地盘｜十二位”，避免称作“天地盘九宫”。
- **大六壬本命与行年**：本命是求测主体基于出生年份形成的长期个人坐标，行年是求测主体在当前年份的补充坐标。二者只补充四课、三传形成的主判断，不能单独推翻主结论，也不得套用于伴侣、合作方等事项相关人。
- **大六壬应期方法**：用户可选择“三传应期法”或“空亡填实法”。三传应期法按初传、中传、末传给出条件候选；空亡填实法只在关键传爻落入日空时适用。应期候选使用“逢某支日／月”等条件表达，不在缺少时间尺度时强报公历日期。应期方法属于分析偏好，不改变课盘身份；旧记录未保存该字段时默认使用三传应期法且不静默回写。
- **天干地支**：中国传统历法的时间表示系统（甲乙丙丁...子丑寅卯...）
- **节气**：二十四节气，用于确定某些占术系统的时间边界
- **农历/阴历**：中国传统历法，与公历（阳历）需要转换

## 相关文档

- 数据库 Schema：`supabase/table_export_from_supabase.sql`
- 前端核心逻辑：`frontend/packages/core/`
- API 规范：`frontend/src/lib/api-utils.ts`
- AI 解读管道：`frontend/src/lib/api/divination-pipeline.ts`

## ADR（架构决策记录）

详见 `docs/adr/` 目录下的具体决策文档。
